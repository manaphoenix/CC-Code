source "https://rubygems.org"

# Specify a Jekyll version that is compatible with GitHub Pages
gem "jekyll", "~> 4.2.0"

# The Just the Docs theme
gem "just-the-docs"

# (Optional) If you want the automatic RSS feed plugin
gem "jekyll-feed"